{
  "buildCommand": "pnpm run build:app",
  "outputDirectory": "dev/.next",
  "installCommand": "pnpm install",
  "framework": "nextjs",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/$1"
    }
  ]
}
